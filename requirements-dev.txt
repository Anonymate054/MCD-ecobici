# Runtime dependencies (included in Lambda deployment packages if needed)
boto3>=1.34.0
requests>=2.31.0

# Development & testing only — NOT deployed to Lambda
pytest>=8.0.0
pytest-cov>=5.0.0
moto[s3,secretsmanager,firehose,ssm,cloudwatch]>=5.0.0
responses>=0.25.0
pandas>=2.1.0
pyarrow>=15.0.0
python-dateutil>=2.9.0
tqdm>=4.66.0
ruff>=0.4.0
